<h1>All Questions</h1>

<% @questions.each do |question| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%=link_to "#{question.title} Resolved: #{question.resolved}", question %>
      </h4>
    </div>
  </div>

  <% end %>
